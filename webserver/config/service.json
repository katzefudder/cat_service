{
  "service": {
    "name": "webserver",
    "tags": ["apache, webserver"],
    "port": 80,
    "checks": [
      {
        "args": ["/usr/sbin/service", "apache2", "status"],
        "interval": "5s"
      }
    ]
  }
}
