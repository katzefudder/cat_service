{
  "service": {
    "name": "grafana",
    "tags": ["grafana"],
    "port": 3000,
    "checks": [
      {
        "name": "HTTP TCP on port 3000",
        "tcp": "localhost:3000",
        "interval": "10s",
        "timeout": "1s"
      }
    ]
  }
}
